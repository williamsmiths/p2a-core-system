# Application
NODE_ENV=development
PORT=3000
APP_NAME=P2A Core System
APP_URL=http://localhost:3000

# Database Master (Write) - Port 5432
DB_MASTER_HOST=localhost
DB_MASTER_PORT=5432
DB_MASTER_USERNAME=p2a_user
DB_MASTER_PASSWORD=Kx9mP2A2024MasterDB7nQ8vR3s
DB_MASTER_DATABASE=p2a_core

# Database Slave (Read) - Port 5433
DB_SLAVE_HOST=localhost
DB_SLAVE_PORT=5433
DB_SLAVE_USERNAME=p2a_user
DB_SLAVE_PASSWORD=Kx9mP2A2024MasterDB7nQ8vR3s
DB_SLAVE_DATABASE=p2a_core

# TypeORM
DB_SYNCHRONIZE=true
DB_LOGGING=true

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=noreply@p2a-asean.org
MAIL_FROM_NAME=P2A ASEAN Platform

# Email Verification
EMAIL_VERIFICATION_EXPIRES_IN=24h
EMAIL_VERIFICATION_URL=http://localhost:3000/auth/verify-email

# Redis (Optional - for caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# CORS
CORS_ORIGIN=http://localhost:3001,http://localhost:4200

# Timezone
TZ=UTC

